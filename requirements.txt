# API
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# LangChain
langchain==0.3.0
langchain-groq==0.2.0
langchain-chroma==0.1.4
langchain-community==0.3.0

# Embeddings (local, lightweight via onnxruntime — avoids PyTorch/CUDA)
fastembed==0.3.1

# Vector DB
chromadb==0.5.0

# PDF parsing
pymupdf==1.24.0

# Config
pydantic-settings==2.5.0
python-dotenv==1.0.0

# Hybrid search (BM25 keyword matching)
rank_bm25==0.2.2

# Testing
pytest==8.3.0
httpx==0.27.0
